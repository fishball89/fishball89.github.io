<%
  const className = typeof class_name !== 'undefined' ? class_name : 'article-tag'
  if (post.tags && post.tags.length){ %>
  <%- list_tags(post.tags, {
    show_count: false,
    class: className,
  }) %>
<% } %>